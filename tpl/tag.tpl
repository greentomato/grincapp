<div class="nativaDataItem tooltip" title="${tooltip}">
    <h3><i class="fa ${icon}"></i>${value}</h3>
    <div class="rangeDots ${rangeClass} ${hasNivel}">
        <label for="">${range}</label>
        ${nivel}
    </div>
    <p class="nativeDataCardItem ${hasDescripcion}">${descripcion}</p>
</div>